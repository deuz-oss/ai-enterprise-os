# Scripts

Skrip operasional untuk deployment dan maintenance akan ditempatkan di sini
seiring kebutuhan. Saat ini seluruh operasi harian dilakukan lewat `Makefile`
di root repo (`make dev`, `make test`, `make lint`, dst).
