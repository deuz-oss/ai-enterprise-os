# Next Sprint Recommendation

1. Implement full CRUD/API generators with schema-aware prompts.
2. Add template conformance tests and golden-file snapshots.
3. Integrate AEOS CLI into CI bootstrap image.
4. Add release automation (`aeos release`) with tagging and changelog generation.
5. Add environment profiles for local/staging/production validation in `aeos doctor`.
