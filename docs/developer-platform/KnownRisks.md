# Known Risks

1. `aeos generate crud` and `aeos generate api` are guidance-mode only in this sprint.
2. Local `aeos doctor` results depend on host-installed tooling.
3. OpenAPI generation assumes each service exposes `app` in `src/*/main.py`.
4. Template evolution requires strict compatibility and migration governance.
