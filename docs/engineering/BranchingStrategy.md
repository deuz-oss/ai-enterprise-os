# Branching Strategy

## Model

- `main` is always releasable.
- Feature branches use `type/short-description` (e.g., `feat/aeos-cli`).
- Rebase/merge from `main` frequently to reduce drift.

## Pull requests

- Small, coherent scope.
- Linked issue/specification required.
- At least one reviewer approval required.
